{
  "name": "polaris-theme",
  "version": "1.0.0",
  "description": "Polaris — rugged premium outdoor & overland Shopify theme (Online Store 2.0)",
  "private": true,
  "scripts": {
    "build": "tailwindcss -i ./tailwind.css -o ./assets/polaris.css --minify",
    "dev": "tailwindcss -i ./tailwind.css -o ./assets/polaris.css --watch",
    "lint": "shopify theme check ."
  },
  "devDependencies": {
    "@shopify/cli": "^3.66.0",
    "tailwindcss": "^3.4.10"
  }
}
